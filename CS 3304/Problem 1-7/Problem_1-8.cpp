#include <iostream>
#include <vector>
using namespace std;
//Implement the set operations covered in class

// A or B
int* unionSet(int* setA, int sizeA, int* setB, int sizeB) {
	int listSize = 0;
	bool x = false; 
	for (int i = 0; i < sizeA < i++) {
		for (int j = 0; j < sizeB; j++) {
			if (setA[i] == setB[j]) {
				x = true; 
				break;
			}
		}
		if (x = true) {
			
		}
		x = false;
	}
}
// A and B
int* intersect(int* setA, int sizeA, int* setB, int sizeB) {
	
}
// The complement of A
int* complement(int* setA, int sizeA, int* setB, int sizeB) {
	
}
// A minus B
int* minus(int* setA, int sizeA, int* setB, int sizeB) {
	
}
// A or B minus A and B
int* symDiff(int* setA, int sizeA, int* setB, int sizeB) {
	
}
// A is at least B
bool subset(int* setA, int sizeA, int* setB, int sizeB) {
	for (int i = sizeA, while 
}

int main() {
	int listU[] = (1, 2, 3, 4, 5, 6, 7, 8);
	int listA[] = {1, 2, 3, 4};
	int listB[] = {1, 2, 3, 4, 5, 6, 7};
	int listC[] = {3, 4, 5, 6};
	
	
	
	return 0;
}